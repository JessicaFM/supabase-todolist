'use client';
import { NextUIProvider } from "@nextui-org/react";

export default function RootLayout() {
  return (
    <NextUIProvider>
      {/* <main>{children}</main> */}
    </NextUIProvider>
  );
}